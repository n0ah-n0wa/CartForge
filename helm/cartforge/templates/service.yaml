apiVersion: v1
kind: Service
metadata:
  name: {{ include "cartforge.fullname" . }}
  labels:
    {{- include "cartforge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cartforge.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
