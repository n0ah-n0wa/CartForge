package com.example.ecommerce.common.persistence;

/**
 * ISO 4217 currency stored as {@code CHAR(3)}. The specified default is EUR.
 * Additional codes may be added when a domain requires them.
 */
public enum CurrencyCode {
    EUR
}
