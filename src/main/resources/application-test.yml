# Automated tests. Store URLs and JWT_SECRET come from the test harness.
# Schema still comes from Flyway; Hibernate only validates.

spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  cors:
    origins: ${CORS_ORIGINS:http://localhost}
