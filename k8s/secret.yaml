# Demo/dev placeholders only. Replace every value before a real deployment.
# Prefer creating the Secret out-of-band, for example:
#   kubectl create secret generic cartforge-secrets -n cartforge \
#     --from-literal=POSTGRES_PASSWORD='...' \
#     --from-literal=JWT_SECRET='...'
apiVersion: v1
kind: Secret
metadata:
  name: cartforge-secrets
  namespace: cartforge
  labels:
    app.kubernetes.io/name: cartforge-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: cartforge
type: Opaque
stringData:
  POSTGRES_PASSWORD: "local-k8s-demo-postgres-password"
  JWT_SECRET: "local-k8s-demo-jwt-secret-32chars-min!!"
